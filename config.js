// LimeCRM Supabase configuration
// Replace both values with your own project values from:
// Supabase Dashboard → Project Settings / Connect → API credentials.
//
// The publishable/anon key is intended for browser apps.
// Security is enforced by the Row Level Security policies in supabase-schema.sql.
window.LIME_CRM_CONFIG = {
  supabaseUrl: "YOUR_SUPABASE_URL",
  supabasePublishableKey: "YOUR_SUPABASE_PUBLISHABLE_KEY"
};
