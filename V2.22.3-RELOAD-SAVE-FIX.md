# v2.22.3 Reload + Save Action Fix

- Fixes desktop navigation appearing behind the login screen after reload/session loss. Desktop `display: ... !important` rules could override the app's `.hidden` state; `#app.hidden` now always wins.
- Adds a bottom Save Production Settings footer to the Admin Website Project workspace.
- Adds a bottom Save Website Information footer to the Employer Website Project Intake editor.
- Both Admin save buttons now enter/leave the busy state together and call the same save operation.
- Uploads remain immediate and separate from Production Settings saves.
- No SQL migration required.
