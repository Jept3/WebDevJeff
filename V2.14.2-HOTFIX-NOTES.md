# Jeffdesign101 v2.14.2 — Admin Stability + Mobile-First Hotfix

## Critical fixes
- Restored missing Admin render functions: Task Inbox, Task Inbox rows, Time Log, Admin Invoices, Billing, and Trash.
- Fixed the reload/session symptom where an Admin session could fall back to Login after a route-render ReferenceError.
- Added guarded page routing: a page-level render failure now stays inside the authenticated app and offers Return Home instead of collapsing the workspace.
- Restored Admin invoice page rendering and preserved invoice preview/view/mark-paid actions.

## Navigation
- Admin and Employer logos are now Home controls.
- Admin logo returns to Dashboard.
- Employer logo returns to Overview.
- Mobile drawer closes automatically when Home/navigation is selected.

## Website Project creation
- Replaced the browser `prompt()` with a native glass modal.
- Includes Project Name, Project Type, optional note, validation, autofocus, and mobile bottom-sheet behavior.

## UI organization
- Added a consistent page shell and page header system.
- Reorganized Task Inbox, Time Log, Invoices, Billing, and Trash with clearer hierarchy.
- Added mobile-first stacking for rows, actions, forms, invoice controls, and timers.
- Improved invoice modal scrolling on phones and made wide invoice tables horizontally scrollable rather than clipping.
- Added safe-area spacing for iPhone/PWA usage.

## Validation performed
- `node --check app.js`: passed.
- Static route scan: 0 missing `render*` references.
- PWA cache bumped to `jeffdesign101-v2.14.2`.
- No database schema change is required for this hotfix.
